syntax = "proto3";

package common.v1;
option go_package = "github.com/pubgo/funk/proto/commonpb;commonpb";

enum VISIABLE_MODE {
  // 默认Namespace可见
  NAMESPACE = 0;
  // 域内可见
  DOMAIN = 1;
  // 全局可见
  GLOBAL = 2;
}
